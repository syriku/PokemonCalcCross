import Foundation
import SwiftCrossUI

struct TypeCalculatorView: View {
    var body: some View {
        VStack {
            Text("Type Calculator")
                .font(.title)
                .padding()

        }
    }
}
