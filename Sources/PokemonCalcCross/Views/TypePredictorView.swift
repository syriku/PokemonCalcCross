import SwiftCrossUI

struct TypePredictorView: View {
    var body: some View {
        Text("Type Predictor")
            .font(.title)
            .padding()
            .padding(.bottom, 20)
    }
}
